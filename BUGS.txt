System -> 
	write_m_func()
	-> input should allow sympy symbol and SYMENGINE SYMBOL
	
	-> Funktion überarbeiten sodass sie als input auch zwei arrays akzeptiert
	
	liniarize()
	-> standard Variablen für einen allgemeine Ruhelage hinzufügen um bei bereits linearen Systemen das eingeben von sinnlosen dummy variablen zu vermeiden

	make_m_funcktion -> params hinzufügen	

	-> init()
	remove is_linear unused ? 

DynamicSymbols
	-> generierung der Symbole (Strings) bei nummerierten Symbolen anschaun, (Klammern ein Problem in Matlab, entfernen ?)




	-> Klasse in zwei Teilen, Möglichkeit schaffen Variablen ohne (t) machen.